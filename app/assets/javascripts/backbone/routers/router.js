/*global Backbone */
var app = app || {};

(function () {
	'use strict';

	// Todo Router
	// ----------
	var BookRouter = Backbone.Router.extend({
	
	});

	app.BookRouter = new BookRouter();
	Backbone.history.start();
})();